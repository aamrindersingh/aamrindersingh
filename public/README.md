# Resume Placeholder

To add your resume:
1. Export your resume as a PDF
2. Name it `resume.pdf`
3. Place it in the `/public` directory
4. The "Download Resume" button will automatically work

Alternatively, you can update the button in `components/sections/hero.tsx` to point to your hosted resume URL.
